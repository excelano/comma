# Values ship reads. The prose is in RELEASING.md; the loop is in
# ~/notes/releasing.md. Format and keys: anderix/ship, docs/PROFILE.md.
loop=hand-cut
version_file=meson.build
version_mirrors=Cargo.toml Cargo.lock data/com.excelano.Comma.metainfo.xml.in
apt_package=comma
deb_count=2
crates=
homebrew=no
winget=
store_lanes=flatpak
