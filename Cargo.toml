[package]
name = "comma"
version = "0.1.10"
edition = "2024"
# The GTK bindings ask for this, and it is well above the 1.85 Debian 13 ships,
# so building from source means a toolchain from rustup rather than from apt.
rust-version = "1.92"
description = "Edit delimited text files"
authors = ["David M. Anderson"]
license = "MIT"
repository = "https://github.com/excelano/comma"
publish = false

[dependencies]
gtk = { package = "gtk4", version = "0.11", features = ["v4_18"] }
adw = { package = "libadwaita", version = "0.9", features = ["v1_7"] }
gettext-rs = { version = "0.8", features = ["gettext-system"] }
zip = { version = "8.6.0", default-features = false, features = ["deflate-flate2-zlib-rs"] }
pangocairo = "0.22.8"
# The spreadsheet address grammar, shared with xled and xshape rather than
# spelled out a fourth time. Comma uses the column letters from it today; what
# it is really for is the day a selection is something a person can name.
xaddr = "0.2"

[profile.release]
lto = "thin"

# What Meson's debugoptimized builds: release code, keeping enough of the debug
# information for a panic to name the line it came from. This is the profile to
# install from source, because an unoptimized Comma spends about a second
# building the cells of a wide file before it can draw it.
[profile.debugoptimized]
inherits = "release"
debug = "line-tables-only"

[dev-dependencies]
zip = { version = "8.6.0", default-features = false, features = ["deflate-flate2-zlib-rs"] }
